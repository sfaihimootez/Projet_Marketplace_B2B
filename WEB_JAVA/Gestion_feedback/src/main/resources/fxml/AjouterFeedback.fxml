<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<VBox xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="gestion_feedback.controller.AjouterFeedbackController"
      alignment="TOP_CENTER" spacing="25" style="-fx-background-color: linear-gradient(to bottom, #f5f7fa, #e8ecf1);">

    <padding><Insets top="40" right="50" bottom="40" left="50"/></padding>

    <!-- En-tête -->
    <VBox alignment="CENTER" spacing="10">
        <Label text="✨ NOUVEAU FEEDBACK" style="-fx-font-size: 32px; -fx-font-weight: bold; -fx-text-fill: #1F4AA8;">
            <font><Font name="System Bold" size="32"/></font>
        </Label>
        <Label text="Partagez votre expérience" style="-fx-font-size: 14px; -fx-text-fill: #666666;"/>
    </VBox>

    <!-- Carte principale -->
    <VBox spacing="25" style="-fx-background-color: white; -fx-border-color: #1F4AA8; -fx-border-width: 2; -fx-padding: 35; -fx-background-radius: 15; -fx-border-radius: 15; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.15), 15, 0, 0, 5);" VBox.vgrow="ALWAYS">

        <!-- Section Note par étoiles -->
        <VBox alignment="CENTER" spacing="15" style="-fx-background-color: #f8f9fa; -fx-padding: 25; -fx-background-radius: 10;">
            <Label text="Votre évaluation" style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #333333;"/>
            <HBox fx:id="hboxEtoiles" alignment="CENTER" spacing="15"/>
            <Label fx:id="lblNoteSelectionnee" text="Sélectionnez une note" style="-fx-font-size: 16px; -fx-text-fill: #666666;"/>
        </VBox>

        <!-- Section Commentaire -->
        <VBox spacing="10">
            <Label text="📝 Votre commentaire" style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #333333;"/>
            <TextArea fx:id="txtCommentaire"
                      prefHeight="180"
                      promptText="Partagez votre avis détaillé ici..."
                      wrapText="true"
                      style="-fx-font-size: 14px; -fx-border-color: #ddd; -fx-border-radius: 8; -fx-background-radius: 8; -fx-padding: 12;"/>
        </VBox>

        <!-- Section Date -->
        <VBox spacing="10">
            <Label text="📅 Date" style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #333333;"/>
            <DatePicker fx:id="dpDate"
                        prefWidth="Infinity"
                        style="-fx-font-size: 14px; -fx-border-color: #ddd; -fx-border-radius: 8; -fx-background-radius: 8;"/>
        </VBox>

    </VBox>

    <!-- Boutons d'action -->
    <HBox alignment="CENTER" spacing="20" style="-fx-padding: 10 0 0 0;">
        <Button text="✓ ENREGISTRER"
                fx:id="btnAjouter"
                onAction="#ajouter"
                prefWidth="200"
                style="-fx-background-color: linear-gradient(to bottom, #4CAF50, #45a049); -fx-text-fill: white; -fx-font-size: 16px; -fx-font-weight: bold; -fx-padding: 15 40; -fx-background-radius: 25; -fx-cursor: hand; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 8, 0, 0, 3);">
            <font><Font name="System Bold" size="16"/></font>
        </Button>
        <Button text="↩ ANNULER"
                fx:id="btnRetour"
                onAction="#retour"
                prefWidth="200"
                style="-fx-background-color: linear-gradient(to bottom, #757575, #616161); -fx-text-fill: white; -fx-font-size: 16px; -fx-font-weight: bold; -fx-padding: 15 40; -fx-background-radius: 25; -fx-cursor: hand; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 8, 0, 0, 3);">
            <font><Font name="System Bold" size="16"/></font>
        </Button>
    </HBox>

</VBox>
