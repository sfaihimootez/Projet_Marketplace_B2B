<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<VBox xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="gestion_feedback.controller.VoirReponsesController"
      spacing="20" style="-fx-background-color: linear-gradient(to bottom, #f5f7fa, #e8ecf1);">

    <padding><Insets top="25" right="25" bottom="25" left="25"/></padding>

    <!-- En-tête -->
    <VBox alignment="CENTER" spacing="8" style="-fx-background-color: white; -fx-padding: 20; -fx-background-radius: 12; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 3);">
        <Label fx:id="lblTitre" text="💬 RÉPONSES AU FEEDBACK" style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: #1F4AA8;">
            <font><Font name="System Bold" size="28"/></font>
        </Label>
        <Label text="Consultez et gérez toutes les réponses" style="-fx-font-size: 13px; -fx-text-fill: #666666;"/>
    </VBox>

    <!-- Liste des réponses -->
    <ScrollPane fitToWidth="true" style="-fx-background-color: transparent; -fx-background: transparent;" VBox.vgrow="ALWAYS">
        <VBox fx:id="vboxReponses" spacing="15" style="-fx-padding: 5;"/>
    </ScrollPane>

    <!-- Section Ajouter une réponse -->
    <VBox spacing="15" style="-fx-background-color: white; -fx-padding: 25; -fx-background-radius: 12; -fx-border-color: #4CAF50; -fx-border-width: 2; -fx-border-radius: 12; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 3);">

        <Label text="✨ Ajouter une nouvelle réponse" style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #333333;"/>

        <VBox spacing="10">
            <Label text="📝 Contenu de la réponse" style="-fx-font-size: 14px; -fx-text-fill: #666666; -fx-font-weight: bold;"/>
            <TextArea fx:id="txtContenuReponse"
                      prefHeight="120"
                      promptText="Écrivez votre réponse ici..."
                      wrapText="true"
                      style="-fx-font-size: 14px; -fx-border-color: #ddd; -fx-border-radius: 8; -fx-background-radius: 8; -fx-padding: 12;"/>
        </VBox>

        <VBox spacing="10">
            <Label text="📅 Date de la réponse" style="-fx-font-size: 14px; -fx-text-fill: #666666; -fx-font-weight: bold;"/>
            <DatePicker fx:id="dpDateReponse"
                        prefWidth="Infinity"
                        promptText="Sélectionnez une date"
                        style="-fx-font-size: 14px; -fx-border-color: #ddd; -fx-border-radius: 8; -fx-background-radius: 8;"/>
        </VBox>

        <HBox alignment="CENTER" spacing="15" style="-fx-padding: 10 0 0 0;">
            <Button text="✓ AJOUTER RÉPONSE"
                    fx:id="btnAjouterReponse"
                    onAction="#ajouterReponse"
                    prefWidth="200"
                    style="-fx-background-color: linear-gradient(to bottom, #4CAF50, #45a049); -fx-text-fill: white; -fx-font-size: 15px; -fx-font-weight: bold; -fx-padding: 12 30; -fx-background-radius: 22; -fx-cursor: hand; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 8, 0, 0, 3);">
                <font><Font name="System Bold" size="15"/></font>
            </Button>

            <Button text="↩ FERMER"
                    fx:id="btnFermer"
                    onAction="#fermer"
                    prefWidth="150"
                    style="-fx-background-color: linear-gradient(to bottom, #757575, #616161); -fx-text-fill: white; -fx-font-size: 15px; -fx-font-weight: bold; -fx-padding: 12 30; -fx-background-radius: 22; -fx-cursor: hand; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 8, 0, 0, 3);">
                <font><Font name="System Bold" size="15"/></font>
            </Button>
        </HBox>

    </VBox>

</VBox>
